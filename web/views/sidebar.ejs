<aside class="sidebar">
  <div class="sidebar-header">
    <div class="bot-avatar">
      <% if (botUser && botUser.displayAvatarURL) { %>
        <img src="<%= botUser.displayAvatarURL({ size: 64 }) %>" alt="Bot">
      <% } else { %>
        A
      <% } %>
    </div>
    <div class="bot-info">
      <h2><%= botUser ? botUser.username : 'Arki Bot' %></h2>
      <span>● En ligne</span>
    </div>
  </div>
  <nav class="sidebar-nav">
    <a href="/" class="nav-link <%= path === '/' ? 'active' : '' %>">
      <span class="icon">🏠</span> Tableau de bord
    </a>
    <a href="/roulette" class="nav-link <%= path === '/roulette' ? 'active' : '' %>">
      <span class="icon">🎰</span> Roulette
    </a>
    <a href="/votes" class="nav-link <%= path === '/votes' ? 'active' : '' %>">
      <span class="icon">📊</span> Votes
    </a>
    <a href="/config" class="nav-link <%= path === '/config' ? 'active' : '' %>">
      <span class="icon">⚙️</span> Configuration
    </a>
  </nav>
  <div class="sidebar-footer">
    <a href="/logout" class="nav-link">
      <span class="icon">🚪</span> Déconnexion
    </a>
  </div>
</aside>
